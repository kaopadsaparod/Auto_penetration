Analyze this {tool_name} output and extract all findings as a JSON list.
Each finding should have: type, detail, severity (low/medium/high/critical).
Output ONLY a JSON array.

--- {tool_name} output ---
{output}
